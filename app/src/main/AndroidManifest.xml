<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Izin untuk menampilkan notifikasi (wajib Android 13 / API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SENTRY">

        <!-- Activity Utama: Dashboard Monitoring -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SENTRY"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity Log Deteksi: Riwayat Deteksi Tembakan -->
        <activity
            android:name=".LogDeteksiActivity"
            android:exported="false"
            android:label="Detection Logs"
            android:theme="@style/Theme.SENTRY" />

        <!-- Firebase Cloud Messaging Service -->
        <service
            android:name=".service.SentryFCMService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Foreground Service: Realtime Database Listener -->
        <!-- Service ini berjalan terus di background untuk mendengarkan
             perubahan /gunshot_status dari ESP32 di Realtime Database.
             Ketika status berubah ke 1 (bahaya), service langsung
             menampilkan push notification ke HP. -->
        <service
            android:name=".service.SentryListenerService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

    </application>

</manifest>